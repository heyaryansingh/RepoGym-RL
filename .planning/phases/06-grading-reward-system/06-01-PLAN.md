---
phase: 06-grading-reward-system
plan: 01
type: execute
depends_on: []
files_modified: [src/repogym/grader/static.py]
---

<objective>
Implement static analysis reward signals using Radon.

Purpose: Provide the agent with feedback on code quality (complexity, maintainability) to discourage "hacky" solutions.
Output: `src/repogym/grader/static.py` with metrics calculation logic.
</objective>

<execution_context>
./.agent/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create static grader module</name>
  <files>src/repogym/grader/static.py</files>
  <action>Create `src/repogym/grader/static.py` and implement:
  - `get_complexity_score(content: str) -> float`: Returns a normalized score based on Radon Cyclomatic Complexity.
  - `get_maintainability_score(content: str) -> float`: Returns a normalized score based on Radon Maintainability Index.</action>
  <verify>python -c "from repogym.grader.static import get_complexity_score; print(get_complexity_score('def f(): pass'))"</verify>
  <done>Static grader module implemented and basic scoring verified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Radon metrics are correctly calculated for sample code strings.
- [ ] Scores are normalized to a manageable range (e.g., 0.0 to 1.0).
</verification>

<success_criteria>
- Reliable static analysis feedback for files modified by the agent.
- Integration-ready metrics for the reward function.
</success_criteria>

<output>
After completion, create `.planning/phases/06-grading-reward-system/06-01-SUMMARY.md`.
</output>
