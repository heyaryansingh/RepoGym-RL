---
phase: 06-grading-reward-system
plan: 02
type: execute
depends_on: ["06-01"]
files_modified: [src/repogym/grader/functional.py, src/repogym/env.py]
---

<objective>
Implement functional reward signals based on test pass/fail deltas.

Purpose: Reward the agent for fixing bugs (tests going from fail to pass) and penalize regressions.
Output: `src/repogym/grader/functional.py` and updated `RepoGymEnv.step` reward logic.
</objective>

<execution_context>
./.agent/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/repogym/env.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement test delta calculation</name>
  <files>src/repogym/grader/functional.py</files>
  <action>Create `src/repogym/grader/functional.py` and implement:
  - `calculate_test_reward(prev_results: dict, current_results: dict) -> float`: 
    - +1.0 for each test that changed from FAIL to PASS.
    - -1.0 for each test that changed from PASS to FAIL.</action>
  <verify>python -c "from repogym.grader.functional import calculate_test_reward; print(calculate_test_reward({'t1': 'FAIL'}, {'t1': 'PASS'}))"</verify>
  <done>Functional reward logic implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate rewards into RepoGymEnv</name>
  <files>src/repogym/env.py</files>
  <action>Update `RepoGymEnv.step` to:
  1. Detect if a `run_tests` action was performed.
  2. If so, parse results and call `calculate_test_reward`.
  3. Combine functional rewards with static analysis rewards from changed files.</action>
  <verify>python -c "from repogym.env import RepoGymEnv; env = RepoGymEnv(); print('Reward integration added')"</verify>
  <done>Rewards integrated into the environment step logic.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Test transitions (FAIL -> PASS) result in positive rewards.
- [ ] Regressions result in negative rewards.
</verification>

<success_criteria>
- Clear and objective feedback loop for task completion.
- Balanced reward signal encouraging both functionality and code quality.
</success_criteria>

<output>
After completion, create `.planning/phases/06-grading-reward-system/06-02-SUMMARY.md`.
</output>
