---
phase: 03-action-space-framework
plan: 02
type: execute
depends_on: ["03-01"]
files_modified: [src/repogym/env.py]
---

<objective>
Implement the action dispatcher in the RepoGymEnv class.

Purpose: Translate agent actions (JSON strings) into executable commands within the sandbox.
Output: Updated `RepoGymEnv.step` method and dispatcher logic.
</objective>

<execution_context>
./.agent/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/repogym/env.py
@src/repogym/actions.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update RepoGymEnv action space</name>
  <files>src/repogym/env.py</files>
  <action>In `src/repogym/env.py`:
  - Update `self.action_space` to `gymnasium.spaces.Text(min_length=0, max_length=10000)`.
  - Import the `Action` union from `repogym.actions`.
  - Implement a `_dispatch_action(self, action_str)` method that:
    1. Parses JSON.
    2. Validates with Pydantic.
    3. (Stub) Routes to sandbox execution (Phase 5 will fully connect this).</action>
  <verify>python -c "from repogym.env import RepoGymEnv; env = RepoGymEnv(); print(env.action_space)"</verify>
  <done>Action space updated to Text and dispatcher stub implemented.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `RepoGymEnv` successfully parses a valid JSON action string.
- [ ] Error handling for invalid JSON or schema violations is in place (returns appropriate info/reward).
</verification>

<success_criteria>
- `RepoGymEnv` uses `Text` action space for JSON-based tool calling.
- Dispatcher correctly identifies and validates action types.
</success_criteria>

<output>
After completion, create `.planning/phases/03-action-space-framework/03-02-SUMMARY.md`.
</output>
