---
phase: 03-action-space-framework
plan: 01
type: execute
depends_on: []
files_modified: [src/repogym/actions.py]
---

<objective>
Define the structured action schemas for the RepoGym-RL agent using Pydantic.

Purpose: Provide a clear, type-safe contract for agent interactions like reading files, writing files, and running tests.
Output: `src/repogym/actions.py` containing Pydantic models for all supported actions.
</objective>

<execution_context>
./.agent/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define base action schemas</name>
  <files>src/repogym/actions.py</files>
  <action>Create `src/repogym/actions.py` and define Pydantic models for the following actions:
  - `ListFiles`: `path` (str)
  - `ReadFile`: `path` (str)
  - `WriteFile`: `path` (str), `content` (str)
  - `RunTests`: `path` (optional str)
  - `RunCommand`: `command` (str)
  
  Include a discriminated union `Action` to wrap these models.</action>
  <verify>python -c "from repogym.actions import ListFiles, Action; print(ListFiles(path='src').model_dump_json())"</verify>
  <done>Action schemas defined and basic serialization verified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `src/repogym/actions.py` exists and is syntactically correct.
- [ ] Pydantic models can validate correct and reject incorrect input.
</verification>

<success_criteria>
- All core actions from PROJECT.md are represented as Pydantic models.
- Actions are easily serializable to/from JSON.
</success_criteria>

<output>
After completion, create `.planning/phases/03-action-space-framework/03-01-SUMMARY.md`.
</output>
