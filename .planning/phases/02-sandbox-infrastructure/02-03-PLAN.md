---
phase: 02-sandbox-infrastructure
plan: 03
type: execute
depends_on: ["02-02"]
files_modified: [src/repogym/sandbox/docker.py]
---

<objective>
Implement file transfer and command execution inside the sandbox.

Purpose: Allow the environment to inject code into the sandbox and retrieve results/telemetry.
Output: Communication and execution methods in `DockerSandbox`.
</objective>

<execution_context>
./.agent/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/repogym/sandbox/docker.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement file transfer</name>
  <files>src/repogym/sandbox/docker.py</files>
  <action>Implement file transfer methods using `put_archive` and `get_archive` (or simplified wrappers):
  - `copy_to(local_path, remote_path)`: Copy a file or directory into the container.
  - `copy_from(remote_path, local_path)`: Extract a file or directory from the container.</action>
  <verify>python -c "from repogym.sandbox.docker import DockerSandbox; ds = DockerSandbox(); ds.create_container('python:3.11-slim', 'sleep 10'); ds.start(); ds.copy_to('pyproject.toml', '/tmp/pyproject.toml'); ds.stop(); ds.remove()"</verify>
  <done>File transfer methods implemented and basic copy verified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement command execution</name>
  <files>src/repogym/sandbox/docker.py</files>
  <action>Implement `execute(command, workdir=None)` using `container.exec_run()`.
  Return: `exit_code`, `stdout`, `stderr`.</action>
  <verify>python -c "from repogym.sandbox.docker import DockerSandbox; ds = DockerSandbox(); ds.create_container('python:3.11-slim', 'sleep 10'); ds.start(); res = ds.execute('echo hello'); print(res.output.decode()); ds.stop(); ds.remove()"</verify>
  <done>Command execution implemented and stdout capture verified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Files can be moved in and out of the container.
- [ ] Commands can be executed and their output captured correctly.
</verification>

<success_criteria>
- Successful bidirectional file transfer.
- Reliable command execution with exit code and output capture.
</success_criteria>

<output>
After completion, create `.planning/phases/02-sandbox-infrastructure/02-03-SUMMARY.md`.
</output>
