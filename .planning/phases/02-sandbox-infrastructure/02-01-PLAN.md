---
phase: 02-sandbox-infrastructure
plan: 01
type: execute
depends_on: []
files_modified: [src/repogym/sandbox/__init__.py, src/repogym/sandbox/docker.py]
---

<objective>
Establish the Docker management module for the RepoGym-RL sandbox.

Purpose: Provide a clean abstraction over the Docker SDK for containerized task execution.
Output: `src/repogym/sandbox/` package and the base `DockerSandbox` class.
</objective>

<execution_context>
./.agent/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sandbox package</name>
  <files>src/repogym/sandbox/__init__.py</files>
  <action>Create the `src/repogym/sandbox/` directory and an empty `__init__.py` file.</action>
  <verify>ls src/repogym/sandbox</verify>
  <done>Directory created and initialized.</done>
</task>

<task type="auto">
  <name>Task 2: Define DockerSandbox class</name>
  <files>src/repogym/sandbox/docker.py</files>
  <action>Create `src/repogym/sandbox/docker.py` and define the `DockerSandbox` class.
  Include:
  - `__init__`: Initialize Docker client using `docker.from_env()`.
  - Class attributes for default image (e.g., `python:3.11-slim`) and resource limits.
  - Basic logging setup for sandbox operations.</action>
  <verify>python -c "from repogym.sandbox.docker import DockerSandbox; ds = DockerSandbox(); print('DockerSandbox initialized')"</verify>
  <done>DockerSandbox class defined and client initialization verified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Sandbox package exists.
- [ ] `DockerSandbox` can be instantiated without errors (assuming Docker is running).
</verification>

<success_criteria>
- Clear separation of sandbox logic from the core environment.
- Docker SDK client correctly initialized from the environment.
</success_criteria>

<output>
After completion, create `.planning/phases/02-sandbox-infrastructure/02-01-SUMMARY.md`.
</output>
