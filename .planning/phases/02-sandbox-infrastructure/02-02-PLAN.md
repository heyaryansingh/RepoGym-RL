---
phase: 02-sandbox-infrastructure
plan: 02
type: execute
depends_on: ["02-01"]
files_modified: [src/repogym/sandbox/docker.py]
---

<objective>
Implement container lifecycle management in the DockerSandbox.

Purpose: Automate the creation, starting, stopping, and removal of isolated execution environments.
Output: Lifecycle methods in `DockerSandbox` class.
</objective>

<execution_context>
./.agent/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/repogym/sandbox/docker.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement lifecycle methods</name>
  <files>src/repogym/sandbox/docker.py</files>
  <action>Implement the following methods in `DockerSandbox`:
  - `create_container(image, command, **kwargs)`: Create a container with specified image and resource limits (mem_limit="512m", nano_cpus=1000000000).
  - `start()`: Start the created container.
  - `stop(timeout=10)`: Stop the container.
  - `remove()`: Remove the container and its volumes.</action>
  <verify>python -c "from repogym.sandbox.docker import DockerSandbox; ds = DockerSandbox(); ds.create_container('python:3.11-slim', 'sleep 10'); ds.start(); print('Started'); ds.stop(); ds.remove(); print('Removed')"</verify>
  <done>Lifecycle methods implemented and verified through a simple container run.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Container can be created, started, and stopped reliably.
- [ ] Resources are cleaned up after container removal.
</verification>

<success_criteria>
- Reliable container lifecycle management.
- Proper handling of Docker SDK exceptions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-sandbox-infrastructure/02-02-SUMMARY.md`.
</output>
