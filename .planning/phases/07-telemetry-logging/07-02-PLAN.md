---
phase: 07-telemetry-logging
plan: 02
type: execute
depends_on: ["07-01"]
files_modified: [src/repogym/telemetry.py, src/repogym/env.py]
---

<objective>
Implement JSONL export for episode trajectories and integrate with RepoGymEnv.

Purpose: Save agent experiences to disk for later analysis or training.
Output: Export logic in `EpisodeLogger` and environment integration.
</objective>

<execution_context>
./.agent/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/repogym/telemetry.py
@src/repogym/env.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement JSONL export</name>
  <files>src/repogym/telemetry.py</files>
  <action>Add `EpisodeLogger.write_jsonl(file_path)` method to serialize the episode steps into a JSONL file.</action>
  <verify>python -c "from repogym.telemetry import EpisodeLogger; logger = EpisodeLogger('e1', 't1'); logger.write_jsonl('test.jsonl')"</verify>
  <done>JSONL export logic implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate telemetry with RepoGymEnv</name>
  <files>src/repogym/env.py</files>
  <action>Update `RepoGymEnv`:
  - Initialize `self.logger = EpisodeLogger(...)` in `reset()`.
  - Log each step in `step()` using `self.logger.log_step(...)`.
  - Export logs on `close()` if a log directory is configured.</action>
  <verify>python -c "from repogym.env import RepoGymEnv; env = RepoGymEnv(); env.reset(); env.step('{}'); env.close()"</verify>
  <done>Telemetry integrated into environment lifecycle.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] A valid JSONL file is generated after an environment episode.
- [ ] The file contains one JSON object per step.
</verification>

<success_criteria>
- Reliable persistence of agent trajectories.
- Standardized format for external tool compatibility.
</success_criteria>

<output>
After completion, create `.planning/phases/07-telemetry-logging/07-02-SUMMARY.md`.
</output>
