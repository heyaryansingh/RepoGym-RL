---
phase: 07-telemetry-logging
plan: 01
type: execute
depends_on: []
files_modified: [src/repogym/telemetry.py]
---

<objective>
Implement a structured logger for and episode steps and metrics.

Purpose: Capture all agent interactions, results, and rewards in a structured, machine-readable format.
Output: `src/repogym/telemetry.py` with `EpisodeLogger` class.
</objective>

<execution_context>
./.agent/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create telemetry module</name>
  <files>src/repogym/telemetry.py</files>
  <action>Create `src/repogym/telemetry.py` and define:
  - `StepLog` (Pydantic model): `timestamp`, `step`, `action`, `reward`, `info`, `observation_summary`.
  - `EpisodeLogger`: A class to collect `StepLog` objects and manage episode-level metadata (`episode_id`, `task_id`).</action>
  <verify>python -c "from repogym.telemetry import StepLog; print(StepLog(timestamp=0, step=1, action='ls', reward=0.0, info={}, observation_summary=''))"</verify>
  <done>Telemetry module and EpisodeLogger implemented.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `EpisodeLogger` correctly accumulates `StepLog` entries.
- [ ] Log entries capture all essential step information.
</verification>

<success_criteria>
- Clear, type-safe representation of agent trajectories.
- Easily extensible for additional metrics.
</success_criteria>

<output>
After completion, create `.planning/phases/07-telemetry-logging/07-01-SUMMARY.md`.
</output>
