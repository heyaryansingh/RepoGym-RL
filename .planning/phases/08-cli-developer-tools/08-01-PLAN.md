---
phase: 08-cli-developer-tools
plan: 01
type: execute
depends_on: []
files_modified: [src/repogym/cli/main.py, src/repogym/cli/shell.py]
---

<objective>
Implement an interactive CLI shell for manual environment interaction.

Purpose: Allow developers to "play" the environment, test actions manually, and debug task deployments.
Output: `repogym shell` command.
</objective>

<execution_context>
./.agent/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/repogym/env.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Click CLI entrypoint</name>
  <files>src/repogym/cli/main.py</files>
  <action>Create a Click-based entrypoint in `src/repogym/cli/main.py` with placeholder commands.</action>
  <verify>repogym --help</verify>
  <done>CLI entrypoint established.</done>
</task>

<task type="auto">
  <name>Task 2: Implement interactive shell</name>
  <files>src/repogym/cli/shell.py</files>
  <action>Implement the `shell` command in `src/repogym/cli/shell.py`:
  - Initialize `RepoGymEnv`.
  - Provide a loop for user input (JSON actions).
  - Print transcript, rewards, and info using `Rich` for formatting.</action>
  <verify>repogym shell --task repogym-internal</verify>
  <done>Interactive CLI shell implemented.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] User can manually enter actions and see environment feedback.
- [ ] Environment lifecycle (reset/close) is correctly managed by the CLI.
</verification>

<success_criteria>
- Seamless human-in-the-loop interaction.
- Visual clarity in terminal output.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cli-developer-tools/08-01-SUMMARY.md`.
</output>
