---
phase: 08-cli-developer-tools
plan: 02
type: execute
depends_on: ["08-01"]
files_modified: [src/repogym/cli/inspect.py, src/repogym/cli/main.py]
---

<objective>
Implement a log inspector CLI tool for viewing episode trajectories.

Purpose: Analyze agent behavior, debug reward peaks/vales, and review episode transcripts.
Output: `repogym inspect <file.jsonl>` command.
</objective>

<execution_context>
./.agent/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/repogym/telemetry.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement log inspector</name>
  <files>src/repogym/cli/inspect.py</files>
  <action>Implement the `inspect` command in `src/repogym/cli/inspect.py`:
  - Parse JSONL logs.
  - Display a summary table of rewards and action types.
  - Allow drilling down into specific steps to see full transcripts.</action>
  <verify>repogym inspect logs/some_log.jsonl</verify>
  <done>Log inspector CLI implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Register inspection command</name>
  <files>src/repogym/cli/main.py</files>
  <action>Add the `inspect` command to the main CLI entrypoint.</action>
  <verify>repogym inspect --help</verify>
  <done>Inspect command registered.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Log files can be loaded and parsed correctly.
- [ ] Rich tables and formatting are used for clear data presentation.
</verification>

<success_criteria>
- Fast and intuitive log analysis.
- Clear visualization of episode metrics.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cli-developer-tools/08-02-SUMMARY.md`.
</output>
