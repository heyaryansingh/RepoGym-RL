---
phase: 04-task-pack-management
plan: 01
type: execute
depends_on: []
files_modified: [src/repogym/tasks.py]
---

<objective>
Implement the Task Pack management and loading system.

Purpose: Provide a way to define, load, and prepare target repositories for simulation episodes.
Output: `src/repogym/tasks.py` with `TaskPack` and `TaskManager` classes.
</objective>

<execution_context>
./.agent/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tasks module</name>
  <files>src/repogym/tasks.py</files>
  <action>Create `src/repogym/tasks.py` and define the following:
  - `TaskPack` (Pydantic model): `id`, `name`, `repo_path_or_url`, `base_commit`, `test_cmd`.
  - `TaskManager`: A class to manage a collection of `TaskPack`s and handle downloading/preparing them.</action>
  <verify>python -c "from repogym.tasks import TaskPack; print(TaskPack(id='t1', name='test', repo_path_or_url='.', base_commit='HEAD', test_cmd='pytest'))"</verify>
  <done>Tasks module created and TaskPack model verified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement simple local loader</name>
  <files>src/repogym/tasks.py</files>
  <action>Implement `TaskManager.load_task(task_id)` which returns a `TaskPack`.
  Pre-seed it with a dummy "internal" task pointing to the RepoGym repo itself for testing.</action>
  <verify>python -c "from repogym.tasks import TaskManager; tm = TaskManager(); print(tm.load_task('repogym-internal'))"</verify>
  <done>Local loader implemented with internal task seed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `TaskPack` correctly stores repository metadata.
- [ ] `TaskManager` can retrieve a task by ID.
</verification>

<success_criteria>
- Clear structure for defining sets of software engineering tasks.
- Extensible mechanism for adding new repositories/benchmarks.
</success_criteria>

<output>
After completion, create `.planning/phases/04-task-pack-management/04-01-SUMMARY.md`.
</output>
