---
phase: 04-task-pack-management
plan: 02
type: execute
depends_on: ["04-01"]
files_modified: [src/repogym/env.py]
---

<objective>
Integrate Task Pack loading into the RepoGymEnv.

Purpose: Allow the environment to be initialized with a specific task, loading the code into the sandbox during reset.
Output: Updated `RepoGymEnv.reset` and initialization logic.
</objective>

<execution_context>
./.agent/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/repogym/env.py
@src/repogym/tasks.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update RepoGymEnv for task selection</name>
  <files>src/repogym/env.py</files>
  <action>In `src/repogym/env.py`:
  - Add `task_manager` to `__init__`.
  - Update `reset(self, seed=None, options=None)` to accept a `task_id` in `options`.
  - When a `task_id` is provided, use `task_manager.load_task(task_id)` to get the `TaskPack` and store it as `self.current_task`.</action>
  <verify>python -c "from repogym.env import RepoGymEnv; env = RepoGymEnv(); env.reset(options={'task_id': 'repogym-internal'}); print(env.current_task.id)"</verify>
  <done>Environment updated to support task selection on reset.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `RepoGymEnv` can be initialized with a specific task ID via `reset` options.
- [ ] The `current_task` is correctly available in the environment state.
</verification>

<success_criteria>
- Seamless integration between task management and the Gymnasium environment.
- Consistent state representation for the active task.
</success_criteria>

<output>
After completion, create `.planning/phases/04-task-pack-management/04-02-SUMMARY.md`.
</output>
