---
phase: 05-environment-lifecycle
plan: 02
type: execute
depends_on: ["05-01", "03-02"]
files_modified: [src/repogym/env.py]
---

<objective>
Connect the action dispatcher to the sandbox for real-time execution.

Purpose: Allow the agent to perform real actions (ls, read, write, run) in the isolated sandbox and receive the results.
Output: Fully functional `RepoGymEnv.step` method.
</objective>

<execution_context>
./.agent/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/repogym/env.py
@src/repogym/actions.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Connect dispatcher to sandbox execution</name>
  <files>src/repogym/env.py</files>
  <action>In `src/repogym/env.py`, update the action handling logic in `step()`:
  - For `ListFiles`: Call `self.sandbox.execute('ls -R {path}')`.
  - For `ReadFile`: Call `self.sandbox.execute('cat {path}')`.
  - For `WriteFile`: Use `self.sandbox.copy_to()` (or a direct file write in the sandbox if supported).
  - For `RunTests`: Call `self.sandbox.execute('{test_cmd} {path}')`.
  - For `RunCommand`: Call `self.sandbox.execute('{cmd}')`.
  
  Capture output/errors and append them to `self.transcript`.</action>
  <verify>python -c "from repogym.env import RepoGymEnv; import json; env = RepoGymEnv(); env.reset(); env.step(json.dumps({'action': {'command': 'run_command', 'cmd': 'echo hello'}})); print(env.transcript)"</verify>
  <done>Actions connected to sandbox execution and results captured in transcript.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Agent can execute shell commands in the sandbox via `step()`.
- [ ] Output from the sandbox is correctly reflected in the observation's transcript.
</verification>

<success_criteria>
- End-to-end action execution from JSON tool-call to sandbox output.
- Accurate capture of stdout/stderr and exit codes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-environment-lifecycle/05-02-SUMMARY.md`.
</output>
