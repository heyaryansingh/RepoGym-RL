---
phase: 05-environment-lifecycle
plan: 01
type: execute
depends_on: ["02-03", "04-02"]
files_modified: [src/repogym/env.py]
---

<objective>
Implement sandbox orchestration within the RepoGymEnv lifecycle.

Purpose: Automate the creation and cleanup of isolated Docker environments for each simulation episode.
Output: Updated `RepoGymEnv.reset` and `RepoGymEnv.close` methods.
</objective>

<execution_context>
./.agent/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/repogym/env.py
@src/repogym/sandbox/docker.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Orchestrate sandbox in reset</name>
  <files>src/repogym/env.py</files>
  <action>In `src/repogym/env.py`:
  - Initialize `self.sandbox = DockerSandbox()` in `__init__`.
  - In `reset()`:
    1. If `self.sandbox.container` exists, call `self.sandbox.remove()`.
    2. Call `self.sandbox.create_container()` and `self.sandbox.start()`.
    3. If `self.current_task` is set, copy the repository code into the sandbox `/workspace` using `sandbox.copy_to()`.</action>
  <verify>python -c "from repogym.env import RepoGymEnv; env = RepoGymEnv(); env.reset(options={'task_id': 'repogym-internal'}); print(f'Sandbox active: {env.sandbox.container is not None}')"</verify>
  <done>Sandbox lifecycle integrated into reset with code loading.</done>
</task>

<task type="auto">
  <name>Task 2: Orchestrate sandbox in close</name>
  <files>src/repogym/env.py</files>
  <action>In `src/repogym/env.py`:
  - Update `close()` to call `self.sandbox.remove()` if a container exists.</action>
  <verify>python -c "from repogym.env import RepoGymEnv; env = RepoGymEnv(); env.reset(); env.close(); print('Cleanup verified')"</verify>
  <done>Sandbox cleanup integrated into environment close.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] A Docker container is created and started when `env.reset()` is called.
- [ ] The container is removed when `env.close()` or a second `env.reset()` is called.
</verification>

<success_criteria>
- Reliable automatic lifecycle management of the sandbox.
- Prevention of container leaks across episodes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-environment-lifecycle/05-01-SUMMARY.md`.
</output>
