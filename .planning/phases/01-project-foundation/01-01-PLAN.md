---
phase: 01-project-foundation
plan: 01
type: execute
depends_on: []
files_modified: [pyproject.toml, src/repogym/__init__.py, tests/__init__.py]
---

<objective>
Initialize the Python package structure and project configuration for RepoGym-RL.

Purpose: Establish a modern, standardized Python project foundation using pyproject.toml and a src-layout.
Output: Project directory structure, pyproject.toml, and basic package initialization.
</objective>

<execution_context>
./.agent/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create directory structure</name>
  <files>src/repogym/__init__.py, tests/__init__.py, docs/README.md</files>
  <action>Create the following directory structure:
  - `src/repogym/`: Core package code
  - `tests/`: Project test suite
  - `docs/`: Documentation
  - `tasks/`: Directory for task packs (as per PROJECT.md)
  
  Add empty `__init__.py` files to `src/repogym/` and `tests/` to make them packages.</action>
  <verify>ls -R src tests docs tasks</verify>
  <done>All directories created and initialized with __init__.py where necessary.</done>
</task>

<task type="auto">
  <name>Task 2: Create pyproject.toml</name>
  <files>pyproject.toml</files>
  <action>Create a `pyproject.toml` file with the following specifications:
  - Build system: `setuptools`
  - Project metadata: name="repogym", version="0.1.0", description from PROJECT.md
  - Dependencies: `gymnasium`, `docker`, `radon`, `pydantic`, `click`
  - Optional dependencies (dev): `pytest`, `black`, `isort`, `mypy`
  - Entry points: (leave empty for now, will add CLI later)</action>
  <verify>cat pyproject.toml</verify>
  <done>pyproject.toml created with correct build-system and dependencies.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Directory structure verified
- [ ] `pyproject.toml` is valid (can run `pip install -e .` theoretically, but no need to do it yet unless validating)
</verification>

<success_criteria>
- Project structure follows src-layout.
- pyproject.toml contains all necessary library foundations (Gymnasium, Docker, Radon).
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-01-SUMMARY.md`.
</output>
